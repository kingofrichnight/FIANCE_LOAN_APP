# Lendwise

A polished, responsive loan portfolio dashboard for independent lenders. Lendwise tracks borrowers, calculates monthly repayments, stores borrower profiles locally, accepts document attachments, enables browser notification permissions, and exports the portfolio as an Excel-compatible CSV.

## Run locally

```bash
python3 -m http.server 4173
```

Then open `http://localhost:4173`.
